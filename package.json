{
  "name": "p2p-cap-project",
  "version": "1.0.0",
  "description": "SAP BTP CAP Project – Procure-to-Pay (P2P) for ABC Manufacturing Pvt. Ltd.",
  "repository": "<Add your GitHub repo link here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/cds-dk": "^7",
    "express": "^4",
    "hdb": "^0.19",
    "@sap/hana-client": "^2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "jest": "^29"
  },
  "scripts": {
    "start": "cds-serve",
    "dev": "cds watch",
    "test": "jest --passWithNoTests"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": ":memory:"
        }
      }
    },
    "features": {
      "fiori_preview": true
    }
  }
}
